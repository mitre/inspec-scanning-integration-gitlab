
.ci:stage:saf:verify:
  # image:
  #   name: mitre/saf:latest
  #   entrypoint: [""]
  stage: verify
  script:
      - docker run -it -v $CI_PROJECT_DIR/reports/raw:/share mitre/saf view:summary -i /share/vanilla*.json
      - docker run -it -v $CI_PROJECT_DIR/reports/raw:/share mitre/saf view:summary -i /share/hardened*.json
