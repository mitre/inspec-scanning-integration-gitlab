
.ci:stage:saf:verify:
  # image:
  #   name: mitre/saf:latest
  #   entrypoint: [""]
  stage: verify
  script:
      - pwd
      - echo $CI_PROJECT_DIR
      - ls -lah $CI_PROJECT_DIR/reports/raw
      - docker run -v $CI_PROJECT_DIR/reports/raw:/share --tty --entrypoint ls mitre/saf
