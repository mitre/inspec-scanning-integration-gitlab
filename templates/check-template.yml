.ci:stage:check:inspec:
  image:
      name: $REGISTRY/ci/inspec:4.21.3
      entrypoint: [""]
  variables:
    CHEF_LICENSE: 'accept-silent'
  stage: check 
  script:
    - inspec check . --format=json > checking-results.json
  allow_failure: true
  artifacts:
    when: always
    paths:
        - $CI_PROJECT_DIR/checking-results.json
  tags:
    - docker 