.ci:stage:check:inspec:
  image:
      name: $REGISTRY/ci/inspec:4.21.3
      entrypoint: [""]
  variables:
    CHEF_LICENSE: 'accept-silent'
  stage: check 
  script:
    - inspec check . --format=json > check-report.json
  tags:
    - docker 